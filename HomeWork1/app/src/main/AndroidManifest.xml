<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.unmeshjoshi.homework1">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Contacts"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ContactActivity" android:label="Create new contact">


        </activity>
        <activity android:name=".ContactListActivity" android:label="Contact List"/>
        <activity android:name=".DisplayActivity" android:label="Display Contact"/>
        <activity android:name=".EditActivity" android:label="Edit Contact"></activity>
    </application>

</manifest>